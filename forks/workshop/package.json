{
  "name": "workshop",
  "version": "1.0.0",
  "private": true,
  "description": "Working With LLMs, restaged as the visual aid for a live 30–60 minute workshop. Same scrollytelling engine as scrolly-essay, presenter chrome added.",
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve --port=8095",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "@fontsource/space-grotesk": "^5.2.8",
    "@fontsource/ibm-plex-mono": "^5.2.7"
  }
}
